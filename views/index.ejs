<!DOCTYPE html>
<html>
  <head>
    <title>MovieWeb</title>
    <style>
      body {
        font-family: "Arial", sans-serif;
        background-color: #f5f5f5;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }
      footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: #0078d4; /* Puedes ajustar este color a tu preferencia */
        color: white;
        font-weight: 500;
        padding: 10px 0;
        font-size: 15px;
        position: fixed;
        bottom: 0;
        left: 0;
        width: 100%;
      }

      .footerRight {
        text-align: right;
        margin-right: 15px;
      }

      .footerLeft {
        text-align: left;
        margin-left: 15px;
      }

      h1 {
        font-size: 36px;
        color: #333333;
        margin-bottom: 20px; /* Agregamos margen inferior para separar del input */
      }

      form {
        text-align: center;
      }

      input[type="text"] {
        padding: 10px;
        width: 300px;
        border: 1px solid #ccc;
        border-radius: 4px;
        font-size: 18px;
        margin-right: 10px;
      }

      button[type="submit"] {
        background-color: #0078d4;
        color: white;
        border: none;
        border-radius: 4px;
        padding: 10px 20px;
        font-size: 18px;
        cursor: pointer;
      }

      button[type="submit"]:hover {
        background-color: #0056b3;
      }
    </style>
  </head>
  <body>
    <h1>MovieWeb</h1>
    <form action="/buscar" method="GET">
      <input type="text" name="q" placeholder="Buscar película..." />
      <button type="submit">Buscar</button>
    </form>
    <footer>
      <div class="footerLeft">Alumnos: Sofía Keczeli, Juan Luis Martinó.</div>
      <div class="footerRight">Universidad Austral – 2024</div>
    </footer>
  </body>
</html>
